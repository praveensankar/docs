---
title: "Introduction"
description: "zk-revoke is a system that limits the amount of time a verifier can track a VC’s revocation status. "
---

<img
  className="block dark:hidden"
  src="/images/hero-light.png"
  alt="Hero Light"
/>

<img
  className="hidden dark:block"
  src="/images/hero-dark.png"
  alt="Hero Dark"
/>

## Prerequisites

Install the following languages: GoLand and Node.js.

Then install:

1. **Hardhat** - Hardhat is used to run a local blockchain network.
2. **Solc, abigen** -  Solc and abigen are used for deploying smart contracts to the hardhat network and interacting with the deployed smart contract.

## <Tooltip tip="">Installing </Tooltip>hardhat

Run the following commands to install hardhat locally:

<CodeGroup>

```bash npm
npm install --save-dev hardhat
```

</CodeGroup>

Install the following plugins:

<CodeGroup>

```bash npm
npm install --save-dev @nomicfoundation/hardhat-toolbox
npm install --save-dev @nomicfoundation/hardhat-ignition-ethers
```

</CodeGroup>

Refer to the official hardhat tutorial if you encounter any errors in the installation of hardhat:

https://hardhat.org/tutorial/creating-a-new-hardhat-project

## Installing solc and abigen

Follow the instructions given in https://goethereumbook.org/en/smart-contract-compile/. The abigen tool can be installed using the following command:

<CodeGroup>

```bash golang
go install github.com/ethereum/go-ethereum/cmd/abigen@latest
```

</CodeGroup>

## Run a local blockchain network

Change the directory to blockchain_hardhat and run a local network:

<CodeGroup>

```bash terminal
cd blockchain-hardhat
npx hardhat node
```

</CodeGroup>

## Run the golang program

<CodeGroup>

```bash golang
go build untrace
```

</CodeGroup>

## Plot results

<CodeGroup>

```bash golang
cd plots
poetry install
poetry run python main.py
```

</CodeGroup>